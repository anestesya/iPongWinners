jQuery(function($){
	//mostra a hora que o tweeet vai ser enviado.
	data = new Date();
	data = data.getHours()+":"+data.getMinutes()
	$(".hora").html(data);	
	
	
	$('#periodos').click(function(){
		var $this = $(this);
		
		if ($this.attr('toggled') == "true") {
			$('#periodo').val('manh&atilde;');
		}
		else if ($this.attr('toggled') == "false") {
			$('#periodo').val('tarde');
		}
		
		return false;
	});	
	
	$('#pontos').click(function(){
		var $this = $(this);
		
		if ($this.attr('toggled') == 'true') {
		    $('#ponto').val('entrou');
		}else if ($this.attr('toggled') == 'false'){
		    $('#ponto').val('saiu');
	   	 }
	
		return false;	
	});

	
	try {
 	   if (window.openDatabase){
	   	db = window.openDatabase("oclockr", "0.1", "BD O Clockr HTML5.", 200000);
		db.executeSql('CREATE TABLE IF NOT EXISTS usuarios(id number, nick TEXT)');
 	   } else {
		$('#a_user').after('<p>Banco não carregado.</p>');
	   }
	   
	} catch(e){
		alert('erro: '+e);	
	}

 	$('#gravar').click(function(){
		$('#usuario').val();		

		return false;	
	})	
});
